% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmf_helpers.R
\name{harmonize_platform_residual}
\alias{harmonize_platform_residual}
\title{Residualize platform effects across source and target(s) before NMF}
\usage{
harmonize_platform_residual(source, targets, assay)
}
\arguments{
\item{source}{A SingleCellExperiment/SpatialExperiment used to fit the NMF.}

\item{targets}{A list of SingleCellExperiment/SpatialExperiment objects.}

\item{assay}{The assay shared by source and targets.}
}
\value{
A list with \code{source} (corrected) and \code{targets} (list of corrected
objects), all subset to the shared genes in a common order.
}
\description{
Restricts source and target(s) to their shared genes (matched by
\code{rowData$gene_name}), stacks them into one matrix, and removes a per-gene
platform location effect by fitting \code{expression ~ platform} for each gene
(platform = dataset of origin) and keeping the residuals re-centred on the
grand per-gene mean. This is equivalent to \code{limma::removeBatchEffect} with a
single batch factor and no covariates. Negative corrected values are clamped
to zero so the result is a valid non-negative input for NMF.
}
\details{
Unlike projection-time z-scoring, this couples the source and target(s): NMF
must be (re)fit on the corrected source, so the resulting model is specific to
the target(s) supplied here and is not a reusable artifact. The single-factor
model \code{~ platform} removes per-gene \emph{location} differences only; per-gene
scale/contrast differences are left intact.
}
\keyword{internal}
